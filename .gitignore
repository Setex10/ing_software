# Dependencias (se descargan con "npm install", nunca se suben a git)
/node_modules

# Build de Next.js
/.next/
/out/

# Variables de entorno (el proyecto no las necesita para correr, pero se
# ignoran por si se agregan en el futuro)
.env
.env*.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema operativo / editor
.DS_Store
*.swp
.vscode/
.idea/
